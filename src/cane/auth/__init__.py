"""ผู้ใช้และการยืนยันตัวตน (spec/09)

ชั้นล่างสุดของโดเมนนี้ไม่มี I/O เลย — `secrets.py` กับ `totp.py` เป็นฟังก์ชันล้วน
ที่เทสต์ได้โดยไม่ต้องมีฐานข้อมูล · ส่วนที่แตะ DB อยู่ที่ `db/repo/` ตามแบบของ repo
และส่วนที่ตัดสิน (login, lockout, สิทธิ์) อยู่ที่ `service.py`
"""

from __future__ import annotations
